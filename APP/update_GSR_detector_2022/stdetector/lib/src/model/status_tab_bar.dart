enum StatusMenuTab {
  // screen_tabs without options (default)
  none,
  // just one item selected, it shows the options [edit, remove]
  options,
  // more than one item selected, it shows the option [remove]
  option
}